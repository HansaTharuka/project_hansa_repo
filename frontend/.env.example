# WealthWise frontend environment (deployment.md §5.1).
# Copy this file to frontend/.env. frontend/.env is git-ignored.

# Backend origin the SPA calls directly; the Vite dev server does not proxy.
VITE_API_BASE_URL=http://localhost:8000
